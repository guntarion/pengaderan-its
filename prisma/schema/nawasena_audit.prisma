// prisma/schema/nawasena_audit.prisma
// Domain: NawasenaAuditLog (NAWASENA-specific audit log, replaces generic AuditLog for scoped ops)
// NAWASENA M01

// ============================================
// ENUMS — Audit
// ============================================

enum AuditAction {
  USER_CREATE
  USER_UPDATE
  USER_DEACTIVATE
  USER_ROLE_CHANGE
  USER_BULK_IMPORT
  USER_EMERGENCY_CONTACT_ACCESSED
  ORG_CREATE
  ORG_UPDATE
  ORG_ARCHIVE
  COHORT_CREATE
  COHORT_UPDATE
  COHORT_ARCHIVE
  PAKTA_VERSION_PUBLISH
  PAKTA_SIGN
  PAKTA_REJECT
  PAKTA_RESIGN_TRIGGER
  WHITELIST_ADD
  WHITELIST_REMOVE
  LOGIN
  LOGOUT
  SESSION_REVOKE
  SUPERADMIN_CROSS_ORG_ACCESS
  FULL_DELETE_USER
  EMERGENCY_BULK_SIGN
  // M02: Master Data operations
  MASTER_DATA_SEED
  MASTER_DATA_SEED_FAILED
  TAXONOMY_META_UPDATE
  KEGIATAN_TOGGLE_ACTIVE
  PUBLIC_CATALOG_TOGGLED
  // M15: Notification operations
  NOTIFICATION_RULE_CREATE
  NOTIFICATION_RULE_UPDATE
  NOTIFICATION_RULE_DELETE
  NOTIFICATION_TEMPLATE_PUBLISH
  NOTIFICATION_PREFERENCE_UPDATE
  CRON_MANUAL_TRIGGER
  // M03: Struktur Angkatan — KP Group, Buddy, Kasuh
  KP_GROUP_CREATE
  KP_GROUP_UPDATE
  KP_GROUP_ARCHIVE
  KP_GROUP_ASSIGN_MEMBER
  KP_GROUP_REMOVE_MEMBER
  KP_GROUP_COORDINATOR_REPLACE
  BUDDY_PAIR_CREATE
  BUDDY_PAIR_SWAP
  BUDDY_PAIR_ARCHIVE
  KASUH_PAIR_CREATE
  KASUH_PAIR_REASSIGN
  KASUH_PAIR_ARCHIVE
  PAIRING_REQUEST_CREATE
  PAIRING_REQUEST_APPROVE
  PAIRING_REQUEST_REJECT
  PAIRING_REQUEST_FULFILL
  PAIRING_REQUEST_CANCEL
  BULK_PAIRING_COMMIT
  // M04: Pulse Harian & Weekly Journal operations
  PULSE_SUBMIT
  PULSE_SYNC_BULK
  JOURNAL_DRAFT_UPSERT
  JOURNAL_SUBMIT
  JOURNAL_AUTO_LOCK_MISSED
  RUBRIC_SCORE_CREATE
  RUBRIC_SCORE_NOTE_UPDATE
  RED_FLAG_TRIGGER
  RED_FLAG_FOLLOW_UP
  RED_FLAG_ESCALATE
  DATA_RETENTION_PURGE
  JOURNAL_KP_ACCESS
  // M05: Passport Digital operations
  PASSPORT_SUBMIT
  PASSPORT_VERIFY_APPROVED
  PASSPORT_VERIFY_REJECTED
  PASSPORT_ENTRY_CANCELLED
  PASSPORT_ENTRY_ESCALATED
  PASSPORT_ENTRY_OVERRIDE
  PASSPORT_ENTRY_RESUBMIT
  PASSPORT_QR_SESSION_CREATE
  PASSPORT_QR_SESSION_REVOKE
  PASSPORT_QR_INVALID_ATTEMPT
  PASSPORT_PHOTO_ACCESS_BY_SC
  SKEM_EXPORT_GENERATED
  PASSPORT_RETENTION_PURGE
  // M06: Event RSVP & NPS
  KEGIATAN_INSTANCE_CREATE
  KEGIATAN_INSTANCE_UPDATE
  KEGIATAN_INSTANCE_STATUS_CHANGE
  RSVP_CREATE
  RSVP_UPDATE
  RSVP_DECLINE
  RSVP_WAITLIST_PROMOTE
  RSVP_WAITLIST_FIFO_SKIP
  EVENT_NPS_SUBMIT
  EVENT_NPS_SUBMIT_REJECTED_DUPLICATE
  EVENT_NPS_SUBMIT_REJECTED_WINDOW
  NPS_TRIGGER_SCHEDULED
  NPS_TRIGGER_CANCELLED
  NPS_TRIGGER_MANUAL
  NPS_INDIVIDUAL_DRILLDOWN
  ATTENDANCE_MARK
  DATA_RETENTION_PURGE_M06
  // M07: Time Capsule & Personal Life Map operations
  TIME_CAPSULE_ENTRY_CREATE
  TIME_CAPSULE_ENTRY_UPDATE
  TIME_CAPSULE_ENTRY_DELETE_ADMIN
  TIME_CAPSULE_SHARE_TOGGLE
  TIME_CAPSULE_ATTACHMENT_UPLOAD
  TIME_CAPSULE_ATTACHMENT_DELETE
  LIFE_MAP_GOAL_CREATE
  LIFE_MAP_GOAL_UPDATE_STATUS
  LIFE_MAP_MILESTONE_UPDATE_CREATE
  LIFE_MAP_MILESTONE_UPDATE_EDIT
  LIFE_MAP_SHARE_TOGGLE
  SHARE_GLOBAL_SETTING_CHANGE
  DATA_RETENTION_PURGE_M07
  PORTFOLIO_VIEW_ACCESS
  // M09: KP & Kasuh Logbook operations
  KP_LOG_DAILY_SUBMIT
  KP_LOG_DAILY_EDIT
  KP_LOG_WEEKLY_SUBMIT
  KP_LOG_WEEKLY_EDIT
  KASUH_LOG_SUBMIT
  KASUH_LOG_EDIT
  KASUH_PULSE_READ
  PEER_DEBRIEF_READ
  M09_LOGBOOK_ACCESS_BYPASS
  RED_FLAG_CASCADE_TRIGGERED
  RED_FLAG_CASCADE_FAILED
  RED_FLAG_REVOKED
  M09_RETENTION_PURGE
  M09_PRECOMPUTE_WEEKLY
  // M10: Safeguard & Insiden operations
  INCIDENT_CREATE
  INCIDENT_UPDATE
  INCIDENT_STATUS_CHANGE
  INCIDENT_CLAIM
  INCIDENT_RESOLVE
  INCIDENT_REOPEN
  INCIDENT_RETRACT
  INCIDENT_ESCALATE
  INCIDENT_NOTE_ADD
  INCIDENT_SUPERSEDE
  CONSEQUENCE_ASSIGN
  CONSEQUENCE_SUBMIT
  CONSEQUENCE_APPROVE
  CONSEQUENCE_REJECT
  CONSEQUENCE_CANCEL
  CONSEQUENCE_DEADLINE_EXTEND
  CONSEQUENCE_PASSPORT_CASCADE
  ATTACHMENT_PRESIGN
  ATTACHMENT_CONFIRM
  ATTACHMENT_DOWNLOAD
  SATGAS_PDF_EXPORT
  PEMBINA_ANNOTATION_ADD
  ESCALATION_FALLBACK_SENT
  // M08: Eksekusi Kegiatan (OC Dashboard) operations
  KEGIATAN_INSTANCE_LIFECYCLE_REVERT
  KEGIATAN_INSTANCE_CANCELLED
  KEGIATAN_INSTANCE_RESCHEDULED
  KEGIATAN_INSTANCE_CAPACITY_RAISED
  KEGIATAN_INSTANCE_CAPACITY_OVERRIDE_SC
  ATTENDANCE_SCAN_SUCCESS
  ATTENDANCE_SCAN_INVALID_SIG
  ATTENDANCE_SCAN_LATE
  ATTENDANCE_SCAN_DEDUPED
  ATTENDANCE_BULK_MARK
  ATTENDANCE_MANUAL_OVERRIDE
  ATTENDANCE_AUTO_ALPA
  ATTENDANCE_SC_OVERRIDE
  ATTENDANCE_SYNC_REJECTED
  KEGIATAN_QR_SESSION_CREATE
  KEGIATAN_QR_SESSION_REVOKE
  OUTPUT_UPLOAD_CREATE
  OUTPUT_UPLOAD_DELETE
  OUTPUT_SCAN_QUARANTINE
  KEGIATAN_EVALUATION_SUBMIT
  KEGIATAN_EVALUATION_PREFILL_OVERRIDDEN
  KEGIATAN_EVALUATION_LATE_SUBMIT
  KEGIATAN_EVALUATION_DELETE_BY_SC
  EVENT_CANCELLED_NOTIF_FAILED
  DATA_RETENTION_PURGE_M08
  // M11: Mental Health Screening operations
  MH_SCREENING_SUBMITTED
  MH_REFERRAL_CREATED
  MH_REFERRAL_STATUS_CHANGED
  MH_REFERRAL_REASSIGNED
  MH_REFERRAL_ESCALATED
  MH_CONSENT_GRANTED
  MH_CONSENT_WITHDRAWN
  MH_DELETE_REQUESTED
  MH_RETENTION_DELETED
  MH_RESEARCH_OPT_IN
  MH_KEY_ROTATED
  MH_AGGREGATE_EXPORTED
  MH_M10_CROSS_REFERRED
}

enum DemographicField {
  IS_RANTAU
  IS_KIP
  HAS_DISABILITY
  PROVINCE
  EMERGENCY_CONTACT
}

// ============================================
// MODEL: NawasenaAuditLog (append-only, dual actor/subject)
// ============================================

model NawasenaAuditLog {
  id             String      @id @default(cuid())
  organizationId String?     // nullable for platform-level actions
  action         AuditAction
  actorUserId    String?     // null for system actions
  subjectUserId  String?     // for role change, pakta sign, etc.
  entityType     String      // "User", "Cohort", "PaktaSignature", ...
  entityId       String      // UUID as string
  beforeValue    Json?       // snapshot before change
  afterValue     Json?       // snapshot after change
  reason         String?     @db.Text // required for role change, cross-org, full delete
  ipAddress      String?
  userAgent      String?     @db.Text
  metadata       Json?       // additional context: file hash for bulk import, target org, etc.
  createdAt      DateTime    @default(now())

  // Relations
  organization Organization? @relation(fields: [organizationId], references: [id])
  actor        User?         @relation("AuditActor", fields: [actorUserId], references: [id])
  subject      User?         @relation("AuditSubject", fields: [subjectUserId], references: [id])

  @@index([organizationId, createdAt(sort: Desc)])
  @@index([actorUserId, createdAt(sort: Desc)])
  @@index([subjectUserId, createdAt(sort: Desc)])
  @@index([action, createdAt(sort: Desc)])
  @@index([entityType, entityId])
  @@map("nawasena_audit_logs")
}
